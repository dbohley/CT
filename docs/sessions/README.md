# Session log

One document per working session: what changed, why, what was measured, what is next.
Newest last.

Start a new one by copying [000-template.md](000-template.md) to
`NNN-YYYY-MM-DD-slug.md`, then add a line here.

| # | Date | Session | Summary |
|---|---|---|---|
| 001 | 2026-08-01 | [bootstrap](001-2026-08-01-bootstrap.md) | Full pipeline from an empty repo: three swappable stages, four signal sources, FFT/OLS identification, harmonic EKF, forecasting, diagnostics, six CLI scripts, 152 tests. Found that the 95% K rule under-fits the RC-piecewise model, quantified the `R` residual-fallback failure mode, and corrected two formula typos in the reference doc. |
| 002 | 2026-08-15 | [rig control](002-2026-08-15-rig-control.md) | Scope deliberately widened from estimator to rig controller: CAN transport for the RH02 adapter, both CubeMars codecs, base and needle axes, ToF and tactile sensors, the four-state insertion procedure, the lead servo, the firing gate and the safety monitor — all runnable end to end against a simulated plant under `SimClock`, 294 tests. Added `unknowns.py`, the machine-checked list of the 41 physical constants still unmeasured. Found MIT mode's ±12.5 rad position limit, the `load/kp` steady-state insertion error, and that the tactile sensor's stroke must exceed the breathing excursion. |
| 003 | 2026-08-17 | [teensy-can-bench](003-2026-08-17-teensy-can-bench.md) | First real hardware: the `CT_sensors` Teensy 4.1 bench sketch (ToF + encoder over CAN1) built and flashed; added `ct-sensor-bench` to read it live through the RH02. Identified the RH02 as an `slcan` device (not `gs_usb`/candleLight) via direct probing, answering an open `unknowns.py` question. Blocked at the physical layer: zero CAN frames reach the RH02 despite the Teensy actively transmitting — leading suspect is a missing CAN transceiver between the Teensy's TTL-level CAN1 pins and the RH02's CAN-H/CAN-L. |
| 004 | 2026-08-20 | [motor-bench-can-protocols](004-2026-08-20-motor-bench-can-protocols.md) | Got all three rig motors (needle, base, phantom) individually bench-testable. Needle/base settled on an unverified-but-proven "Gimbal Motor II" position/velocity protocol; phantom confirmed on real CubeMars servo mode. A long MIT-mode detour for the needle (prompted by the GUI showing a different CAN ID) never produced clean motion despite several sound fixes and was abandoned in favor of the proven protocol — root cause still open. Fixed a real bug in `CubeMarsMIT.parse()` (id/fault-code nibbles conflated), 295 tests passing. Hit and resolved a stuck-adapter-needs-USB-replug issue, a silently-overridden `CAN_CHANNEL` duplicate-assignment bug (twice), and a phantom power-connection fault. Physical CAN topology swapped twice mid-session and is not yet documented outside this session. |
| 005 | 2026-08-27 | [approach-bench-and-gl2-telemetry](005-2026-08-27-approach-bench-and-gl2-telemetry.md) | Built sensor-noise and approach-to-contact bench scripts plus the first JSONL-to-figure plotting script. A stall-on-contact overshoot bug led to a web-research pass that found CubeMars's actual GL-II manual, confirming the Gimbal protocol (previously unverified) is real and that its feedback frame — a per-command ACK, not a continuous broadcast, `motor_replies_seen=4` in one 19.6s run — genuinely works. One well-timed real reply found the true root cause: dead-reckoning the hold position from t=0 ignored the motor's real acceleration ramp-up, accumulating a 0.17rad (4.44mm) error that made the "hold" command accidentally ask for more travel, not a stop. Fixed via periodic command resending (denser real telemetry) plus computing the hold target from the latest real reply instead of full-elapsed dead reckoning. Also fixed a live range-mismatch bug in `listen_needle_motor.py` (MIT-mode ranges left over from the abandoned detour) and a stale channel default in `scan_can_bus.py`. |
| 006 | 2026-09-01 | [breathe-profile-extraction](006-2026-09-01-breathe-profile-extraction.md) | First real human breathing data in the repo. Identified the reduction hidden in the raw 70-column OptiTrack takes — the arithmetic mean of the nine `Unlabeled` markers' Y position — and *proved* it against the hand-produced Moira reference to 3.44e-06 mm before applying it anywhere. Added `scripts/build_breathe_profiles.py`, which selects marker columns from each file's own header rather than by index and re-runs that reference check on every invocation, and wrote seven full-length 120 Hz `time_s,y_mm` traces into `breathe_profiles/`. All seven pass through `ct-identify` at 10.2–22.0 bpm. Found that real chest-wall excursion is only 4–8 mm peak-to-peak (an order of magnitude under `configs/sinusoid.yaml`), that the 95% energy rule picks K=1 on every real subject, and that Derek's rate drifts 20.7→14.0 bpm within one take — the `sinusoid_ramp` failure mode, now observed on real data, costing two-thirds of his recovered amplitude. |
| 007 | 2026-09-01 | [phantom ground truth](007-2026-09-01-phantom-ground-truth.md) | Made an `approach_and_seat` run show what the phantom *actually did* beside what the sensor thought it did, as the baseline the FFT/EKF stage will be scored against. `run_breathing_profile.py` now drains RX and logs `measured_mm` from the motor's own status broadcast (previously every status frame was discarded and the RX buffer grew unbounded); `compare_logs` gained a phase filter, without which the bench numbers are not weaker but meaningless — correlation 0.039 across a whole run vs 0.961 over `standoff_hold`. Cross-checking against Stage 1 caught a real bug in the amplitude metric: projecting onto the *unshifted* phantom folded the 677 ms delay into the scale, reporting 0.203 where the truth is 0.326 (confirmed independently by `A_1` = 0.346 mm sensed vs 1.063 mm true). Fixing it dropped the reported residual RMSE from 0.652 mm to 0.072 mm. Headline result: **the tactile chain sees ~1/3 of real excursion, 677 ms late, but reproduces the waveform at r = 0.961 once both are removed**, and the estimator recovers the same fundamental from sensor and truth to 0.16%. New `configs/bench_aligned.yaml` and a per-run `aligned.csv` let the estimator run on real bench data; `tests/test_phantom.py` is the first coverage `compare_logs` has ever had. 311 tests. |
| 008 | 2026-09-01 | [estimator on bench data](008-2026-09-01-estimator-on-bench-data.md) | First run of the FFT identifier and harmonic EKF on real hardware data, with the result on the figure. **Forecasting by the measured 677 ms sensor lag removes 62% of the lag error** (0.0888 mm RMSE vs 0.2332 for reading the sensor late) — the first end-to-end evidence that the repo's premise holds on real data. The horizon is cheap and the lag is expensive: predicting 677 ms ahead costs +0.031 mm and buys back 0.144 mm. One column made it work — `aligned.csv` now writes `y_clean`, which `CSVSource` and `truth_function` already understand, so the forecast is scored against the **phantom** rather than against the sensor with no new plumbing. Three placeholders replaced with measured values (new `R_override` on the identifier, K=3 against the 95% rule's K=1 which costs a measurable 49% in forecast RMSE, h = measured tau_s). The base-motor panel is gone and ground truth defaults to `measured_mm`, so the latency on the graph is the sensor's and not the phantom's. A prediction of mine was wrong in a useful way: the measured free-air R moved NIS 0.040 → 0.243 rather than overshooting 1, which says the remaining inconsistency lives in **Q and the model, not the noise floor**. 313 tests. |
| 009 | 2026-09-01 | [approach travel exhaustion](009-2026-09-01-approach-travel-exhaustion.md) | A run faulted with `no contact within 45s of approach`; the base had actually *arrived* at its 40 mm target at t=13 s and sat there for 32 s with no way to say it had run out of travel. Investigating found that **the rig had been operating on a 6% contact margin all along** — the one previously successful run contacted at exactly its 40.01 mm limit with a peak reading of 0.00941 against a 0.01 threshold. What tipped it over: **real subject profiles carry ~1.5 mm of slow baseline wander that a short looping profile does not** (emma is 618 s and completes zero loops in a 180 s run, so its played slice sat ~1 mm further out than breathing_profile_1's looped mean). Fixed by making `--travel-mm` an initial target that auto-extends to a new `--max-approach-mm` cap, with arrival judged from motor replies rather than dead reckoning, and failure modes that name distance instead of time. Threshold deliberately left at 0.1 mm: the pre-contact baseline maximum (0.0255 mm) already equalled the post-contact settled mean (0.0246 mm), so a lower threshold would have fired before contact. Also closes a question open since session 007 — **`measured_mm` is real at 51.1 Hz**, every tick, with the phantom tracking its own command to 99.1%. 313 tests. |
| 010 | 2026-09-02 | [phantom relative playback](010-2026-09-02-phantom-relative-playback.md) | The phantom used to traverse to the motor's absolute zero before playing any profile, so the bench had to be set up near that one spot. Playback is now referenced to wherever the motor is parked (`initial_rad + profile_offset`), shrinking the ramp-in from an arbitrary traverse to 0.5–1.1 mm. Chose a **mean-referenced datum over a start-referenced one**: every recorded profile starts near its top, so start-referencing would leave the phantom ~1 mm behind where it was parked — the same magnitude that consumed the base's contact margin in session 009. A failed position read is now fatal rather than falling back to absolute zero, which was the exact behaviour being removed. Also fixed something not introduced here but newly reachable: **a phantom subprocess dying mid-run was invisible to the controller**, which would keep seating against a stationary surface and complete a run that looked fine and measured nothing. Log frame stays run-relative so `compare_logs` is unaffected. 313 tests; hardware verification still pending. |
| 011 | 2026-09-02 | [phantom SET_ORIGIN](011-2026-09-02-phantom-set-origin.md) | Session 010's fix did not hold — the phantom still traversed on hardware — and **the diagnosis was impossible because of a regression 010 introduced**: making `measured_mm` relative to the read reference shifted the commanded and measured series together, hiding the traverse; the summary carrying the one decisive number was built after `try/finally` and never written; and the phantom's stdout went to scrollback. Fixed by removing the dependency rather than correcting the reading: `codec.zero()` (`CMD_SET_ORIGIN`) makes the motor's parked position *become* 0, and the profile's offsets are commanded literally, so no reference number is left to be wrong. **The readback after zeroing is the safety check** — the motor must report ~0, which is exactly the property playback needs (commanding 0 must mean "stay put"); if the origin silently fails the run refuses instead of dragging the phantom across the bench. Verified against a mock bus with the motor parked 500° (113 mm of belt) off zero: origin-takes proceeds with a 1.02 mm ramp, origin-fails refuses naming the 113 mm it declined to move. Summary now written from `finally` (including on refusal) with `pre_zero_position_rad`, and the phantom's stdout is captured to a file. 313 tests; hardware verification still pending. |
| 012 | 2026-09-02 | [tactile tare](012-2026-09-02-tactile-tare.md) | Contact was firing on the first sensor sample of **every** run (`contact_t` ≈ 0.0002 s), so APPROACH never ran. Cause: the tactile firmware zero **drifts ~0.008 mm/hour** — 0.0048 mm at rest on Sep 1 16:54, 0.1967 mm on Sep 2 15:46, a 40× creep — while its std stayed at 0.002–0.014 mm. The reading is clean; the datum moves, and it crossed the fixed 0.1 mm threshold. Fixed by taring per run: sample 2 s with the base stationary before any motion and subtract that from the **signed** `dist_cm` before taking the magnitude. Replaying all eight real runs confirms it: the two broken runs get a real approach back (0.0002 s → 5.709 s) while every genuine historical contact is preserved to within 0.5 s. Warn-only when the arm looks already in contact (free arm p2p 0.016 mm vs 0.808 mm riding the phantom). Also fixed two session-011 phantom bugs found by reading the newly-captured `phantom/stdout.log`: the post-SET_ORIGIN readback was reading **stale queued frames** (`bus.recv()` returns the oldest, and ~25 pre-command frames sit ahead of it) causing a false refusal, and Ctrl+C during either ramp escaped the handler, skipped the `disable` frame and left the motor holding. 313 tests; hardware verification still pending. |
| 013 | 2026-09-02 | [lag decomposition](013-2026-09-02-lag-decomposition.md) | `tau_s = 0.677s` was never credible for a sensor whose frames arrive every 8ms, and it turns out not to be sensor latency at all. The ToF is non-contact but shares the CAN bus, the tick loop and the motion, so scoring it splits the chain: **it lags 0.011-0.098s where the tactile arm lags 0.282-0.696s**, and the 0.19-0.62s difference is viscoelastic settling in the *contact*. That term is not constant — it tracks how hard the arm is seated (**+0.63 correlation with seat depth for the lag, −0.84 for the amplitude ratio**), so pressing harder makes the sensing worse in both respects and the lightest seat on record is also the best measurement. The lag explains almost none of the attenuation: a *static* gain of 0.82 down to 0.19 carries it, with stiction (6.8% → 16.3% of samples exactly stationary) compounding. The horizon is now assembled from the run being plotted rather than frozen in a config, via `horizon_from_components`. Fixed four defects in the measurement itself — no sub-sample resolution, an unnormalised correlation, no half-breath ambiguity guard (a ±3s ToF scan really did return −2.77s), and assumed sensor polarity (the ToF anti-correlates and pinned to the search edge). The biggest find was incidental: **the EKF loses frequency lock on real subject profiles and `y_pred` hides it completely** — run 160517 tracks 4.55 ± 3.60 bpm against a true 10.34 with `phi_1` spinning 9.70 rad to absorb the error, so the measurement still fits perfectly and only the forecast is wrong, advancing 0.031s of a requested 0.279s. Session 008's +62% headline holds *only* on the synthetic looping profile, which has zero baseline wander; every emma run loses lock. Estimator panels cropped to the tracked window, recoloured and separated. 317 tests. |
| 014 | 2026-09-03 | [standoff convergence](014-2026-09-03-standoff-convergence.md) | A run took 13 standoff fine steps with 6 retreats over 325s and had to be cancelled, where the run five minutes earlier settled in 3. The trigger was a one-line `DEFAULT_STANDOFF_DIST_CM .8 -> .9`, but it only bit because the fine loop was already marginal three ways. **`max` over a time window is a biased estimator of a noisy periodic peak, and the bias grows with the window** — replayed over both runs' stationary measurement segments it read +0.48 and +0.54mm high (worst +1.77mm) against a 0.30mm tolerance, so a controller that retreats on overshoot backed away from positions that were actually short, and "measuring more carefully by waiting longer" made it worse. Replaced with the mean of counted whole breaths (`BreathPeakWatcher`), which is unbiased; the accept band is now two-sided so an over-read no longer costs a base move; the "two consecutive windows agree" settle gate is gone (real breath-to-breath variation defeated it, and the fixed settle timer already did the job). Also found the window was never the number of breaths it claimed — `min_breaths * nominal_breath_s` = 2 x 4.0s against a real 5.51s period is 1.45 breaths. Replay: the good run is untouched (advance, advance, accept), the failing one drops from 7 retreats to 3, and the three that vanish are exactly the spurious ones. Separately, nothing moves the base at exit, so a run ended with the arm pressed into the phantom and the **next** run tared a moving arm (p2p 8.330mm vs 0.014mm free) — contact at t=0.006s, no APPROACH at all, seat accepted at 0.219mm. A loaded tare now refuses instead of warning; the base is retracted by hand between runs, not by the script (an automatic retraction step was drafted and removed at the user's direction as unrequested autonomous motion). Standoff can give up after `--standoff-max-steps` with a named fault, the way approach has since session 009. 322 tests. |
| 015 | 2026-09-03 | [EKF tuning analysis](015-2026-09-03-ekf-tuning-analysis.md) | Investigated three things about run `20260903-171153` without moving any hardware: whether its 0.349s tactile delay is credible, why the EKF tracks troughs worse than the rest of the waveform, and whether `NIS mean = 0.052` / `frequency lock 10.41 ± 0.92 vs 11.405 bpm` can be tuned. Confirmed no ground-truth leakage (estimator input is strictly `sensor_mm`, traced through `CSVSource` and `run.py`) and built `plot_lag_detail.py`, which shows the sensor visibly trailing truth by 0.35s every breath, snapping into alignment once shifted back — steady-state viscoelastic contact settling, not sensor electronics. The trough theory I proposed (measurement Jacobian vanishing symmetrically at extrema) was **wrong**: bucketing by `\|H_theta\|` shows the highest-magnitude quartile has the *lowest* error; the real pattern is asymmetric (trough RMSE 1.5x peak), pointing at contact loss or hysteresis instead. Swept `identifier.params.q_scale` and found a sharp, non-monotonic transition — frequency lock is fully broken below `q_scale≈0.40` and recovers (unevenly) above `0.42`; adopted `q_scale=0.5`, which measurably improved every metric at once (NIS 0.052→0.092, lock std 0.92→0.49bpm, forecast RMSE 0.2056→0.1747mm, lag error removed 33.9%→43.9%) but is flagged as run-specific pending a sweep across the other six bench runs. Also found this run already had 16 calibration breaths, ruling out "too few breaths" as the standing NIS explanation, and that no bench run has an in-contact `R` opportunity because `run_approach_and_seat.py` never implements the breath-hold step `rig_bench.yaml` configures for the real procedure. 322 tests. |
| 016 | 2026-09-03 | [param sweep tooling](016-2026-09-03-param-sweep-tooling.md) | Session 015's `q_scale=0.5` fix, checked against a second real trial of the same profile (`20260903-185614`): frequency lock is broken there at **every** `q_scale` from 0.3 to 1.0, including the old default — a real result, not a regression `q_scale=0.5` caused, and direct proof one run cannot answer whether a tuning choice generalizes. Built the tooling to answer it properly: `run_approach_and_seat.py` gained a bounded `--retract-only-mm` mode and a `--yes` flag (both explicitly requested, at a specific bounded distance, clearing the bar session 014's refused auto-retract set); new `scripts/collect_param_sweep_runs.py` is a bench orchestrator the user runs themselves that loops 5 profiles x 2 trials, auto-retracts 20mm between attempts, and auto-discards/retries a trial whose `summary.json` shows near-instant contact or an incomplete run (thresholds checked against all six real runs on disk); new `scripts/sweep_ekf_params.py` sweeps `q_scale x omega_bounds` over any collected runs, identifying once per `q_scale` and tracking once per `omega_bounds` fraction, applies a safety-first rule (disqualify anything that breaks lock on *any* run), and says plainly when nothing is safe rather than picking a least-bad option. Extracted `is_frequency_locked()` into `ct.diagnostics.metrics` so the live report and the sweep tool can't define "locked" differently. Smoke-testing the sweep tool on the two existing runs (n=2, not the real answer) surfaced a lead: `omega_bounds=±10%` alone keeps both runs locked at *every* `q_scale` tested, something no `q_scale` value alone could do — worth prioritizing once the real 10-trial sweep runs. 322 tests. |
| 017 | 2026-09-04 | [single-trial collector](017-2026-09-04-single-trial-collector.md) | Replaced session 016's all-in-one `collect_param_sweep_runs.py` (loops 5 profiles x 2 trials automatically with internal retry) with `scripts/collect_sweep_trial.py`, one trial per invocation, at the user's request — a bad trial is now just "run the same command again" rather than something an automatic loop has to detect, and a human watches every physical move rather than an unattended batch. Auto-picks the next trial number per profile, keeps `run_approach_and_seat.py`'s own confirmation (no longer skipped), prints a plain GOOD/DISCARD verdict, retracts 20mm either way. New `--list` mode prints exactly the remaining commands needed to reach the target trial count per profile, always current to what's actually on disk. Runs now land in `outputs/param_sweep_runs/<profile>/trial_<n>/`, accumulating across invocations rather than a fresh timestamp each time. Extracted `resolve_profile_name`/`validate_trial` into `scripts/_sweep_common.py` so the collector and `sweep_ekf_params.py` share one definition; the sweep tool no longer depends on a manifest, instead validating every discovered run directory directly. 322 tests. |
| 018 | 2026-09-04 | [real sweep results](018-2026-09-04-real-sweep-results.md) | The first real 10-run sweep (5 profiles x 2 valid trials) confirms session 016's n=2 lead at full scale: `q_scale=0.5` + `omega_bounds` at ±10% of Stage 1's own rate is the only setting of 44 tested that keeps frequency lock on all 10 real runs (mean RMSE 0.293mm) — and **the currently-configured `q_scale=0.5` alone keeps lock on only 2 of these 10**, so session 015's fix was real but incomplete. Fixed with new `ct.run.resolve_tracker_params()`, which resolves a config's `omega_bounds_fraction` into an absolute bound at runtime from that run's own Stage-1 rate (a fixed rad/s range can't cover subjects at 10-22bpm) — wired into `run_pipeline`/`sweep_horizons`/`ct-track`, and `configs/bench_aligned.yaml` updated. Caught and fixed a real bug this created in `sweep_ekf_params.py` mid-session: it stripped the resolved `omega_bounds` key between grid points but not the new `omega_bounds_fraction`, so every "unset" grid point silently inherited the config's ±10% anyway (the tell: its RMSE was bit-identical to the real `0.1` result). Also: removed 14 invalid trial folders with a new list-then-`--delete` tool (`clean_bad_trials.py`) rather than a repeat of the `rm -rf` that destroyed a real trial earlier this week; built `plot_ekf_detail.py`, a zoomed two-panel visual showing the frequency-lock fix (baseline collapses from 11.4 to under 1bpm over 90s; tuned holds 10.40±0.17) *and* the honest forecast RMSE (worse than the raw sensor on this same run, reported as such, not hidden); and confirmed "massive FFT vs autocorrelation differences" noticed during the sweep are real but a volume effect (~100+ warnings per sweep run, each with a distinct embedded percentage) rather than any single alarming value — headline disagreement across the 10 runs is 0.3-10.8%, with junrong and derek closest to the 10% threshold, derek matching session 006's already-known non-stationary-rate finding. 322 tests. |
| 019 | 2026-09-05 | [needle step response](019-2026-09-05-needle-step-response.md) | Measured `servo.needle.plant` (a session-002-era placeholder blocking `ProcedureState.INSERT`), re-derived the lead compensator against it, and built a simulation to see the difference — all three landed. Along the way: fixed a wrong fit target (the reply's own `motor_velocity_rad_s` reads 6.7-8.5x too high, ~uncorrelated with real motion — switched to differentiated position) and **a real timing bug** where the retract phase shared the step phase's fixed duration, so whenever step velocity exceeded retract capacity the *next* rep started mid-reversal, not from rest — this fully explained a batch of fits that had degenerated to `wn≈300, zeta≈0.026`. Even after that fix, only the very first commanded step of any sequence gave a clean fit (every later rep degenerate a different way each time, mechanism unknown) — per the user's explicit "helps a little, doesn't need to be perfect" steer, used the four independent clean trials anyway: **K=0.92, wn=27.0, zeta=0.35**, then designed **zero=1.0, pole=5.0, gain=12.74** against it (`scripts/design_needle_lead.py`, a grid search rather than single-target bisection since this plant's margin-vs-gain relationship isn't monotonic). Both now in `configs/rig_bench.yaml`, confirmed via `ct-unknowns` as measured, not placeholder. **Biggest find came from the sine-comparison script's own self-check**: `ct.control.states.insert._hold_standoff()` commands `reference + correction` (feedforward-plus-feedback, by its own docstring), not the unity-feedback structure `ct.control.servo.closed_loop_response()`/`residual_lag()` assume — simulating the real `LeadServo` in its actual loop measured an 18% amplitude and ~50% lag discrepancy from the analytical prediction, consistent with the algebra (`G(1+C)/(1+GC)` vs. the standard `GC/(1+GC)`). Not fixed this session — flagged as the most consequential open question in the project, since `tau_cl` feeds every past forecast-horizon conclusion. 322 tests, all pre-existing. |
| 020 | 2026-09-07 | [in-tissue needle identification](020-2026-09-07-in-tissue-needle-identification.md) | Redid session 019's plant measurement in tissue (needle already seated in the phantom) rather than free air, and chased its unresolved Finding 3 (only the first commanded step in any run fits cleanly). Two specific, falsifiable hypotheses for that mystery were tested and **both eliminated**: tissue creep (ruled out — the identical pattern reproduces in free air, where nothing can creep) and stale mode-entry state (ruled out — resending `CLEAR_ERRORS`+`ENTER_MODE` before every rep via new `--reenter-mode-per-rep` still left reps 1-4 degenerate, just with a different failure signature than before). Fell back to brute-force replication instead: 10 independent cold-start trials, one a 30-standard-deviation outlier (dropped), the other nine tightening the estimate to `K=0.900±0.025, wn=27.2±3.7, zeta=0.306±0.095`. **Headline result: this matches free air's `wn=27.1±0.8` almost exactly** — the earlier-looking in-tissue/free-air discrepancy (n=4: 39.2 vs 27.1) was a small-sample noise artifact, not a real tissue effect, so `configs/rig_bench.yaml` was left unchanged. Also settled, by direct algebra, why a user-supplied mass couldn't give an independent second plant estimate to compare against the empirical fit — mass cancels exactly when solving for stiffness/damping from the same `wn`/`zeta` and recombining, so it's a relabeling, not new information. A `--tissue-margin-mm` safety guard was added, then explicitly reverted at the user's request once the existing defaults were confirmed already safe. 322 tests, all pre-existing. |
| 021 | 2026-09-07 | [lead compensator hardware validation](021-2026-09-07-lead-compensator-hardware-validation.md) | Built a live A/B hardware test (`run_needle_lead_tracking_live.py`) to validate session 019's lead compensator for real, and it overshot badly — chasing that down surfaced a real production-code units bug (`_hold_standoff()` reused the velocity ceiling `v_max_mm_s` as the correction's position-magnitude clamp), which led to giving `LeadServo` a genuine rate limit (not just the existing magnitude clamp) and extending the compensator from standoff-hold alone into `AdvanceState`'s increments and `InsertState._drive()`, at the user's explicit direction that its job is general tracking fidelity. Even with both fixed, hardware still oscillated; a "gain amplifies sensor noise" theory was built, tested with a new noise-floor script, and **rejected** — the noise floor measured tiny (0.005mm) while the real driver, confirmed from the uncompensated phase, was dynamic tracking lag (0.139mm std) from chasing a position reference that only updated at the bench script's default 20Hz. A lower-gain redesign (gain 3.58 vs. 12.74, via a new noise-aware constraint added to `design_needle_lead.py`) was computed but never adopted, because testing at 100Hz and then 200Hz — the *actual* production loop rate — made the oscillation vanish and let the **original, unmodified compensator show a real, measured hardware improvement**: 0.0466mm vs. 0.0502mm RMSE (+7.2%), zero saturation. The bench test had been validating the compensator at roughly 1/10th its real operating rate the whole time. 326 tests. |
| 022 | 2026-09-09 | [needle gating live](022-2026-09-09-needle-gating-live.md) | Took the needle-actuation feature to hardware on the sinusoid phantom and got the gated advance completing. Repaired the sensor decode first — collaborators reflashed the Teensy, changing the CAN payload from `<Hfh>` to `<ff>` (angle field dropped, confirmed dead downstream), across five independent decode sites. Built the live-run tooling (`generate_sinusoid_profile.py`, `slice_breathing_profile.py` — the phantom driver loops the *whole* file, so playing "the last 300s" of a real profile needs an actual slice) and added gate-fire/activation markers plus `needle_floating` shading to `plot_approach_and_seat.py`. **Confirmed the re-arm sleeps were dead weight**: `0.1s`+`0.5s` copied into every script that does the `CLEAR_ERRORS`/`ENTER_MODE` handshake, never justified, and `--rearm-sleep-s 0.0` runs clean. Then spent most of the session on the gated advance burning all five attempts on ~6ms aborts, through **four bugs in sequence** — frozen `tracker.state` during the paused drive; a units bug in that fix (`elapsed` is rebased, `tracker.t` is raw monotonic, giving `dt=-1.2e6s` that still returned plausible in-range values because `theta` only enters via `sin`/`cos`); a **structural** mismatch where the gate fires on `forecast(t+h)` while the abort tested `value(t)`, so any `inhale_abort_frac` below where `value(t)` sat at fire time tripped instantly and **no threshold value could fix it**; and finally the model check, retired but left wired as a parallel `or`, still deciding every abort. Replaced with a model-free raw-sensor rule (reference the reading at drive start, require a descent to arm, float on return — symmetric about the trough), extracted into a pure `advance_abort_decision()` so the offline replay exercises the *shipped* code. **The real find came last**: run 7 completed but fired at peak inhale, because the tracker had collapsed to half rate and gone antiphase — and the premise behind pausing it ("actuation corrupts the sensor") is **false**, the raw signal being clean straight through both drives (`5.76→3.99→8.40`). Removed the pause entirely (largest step 6.09s → 0.045s) and wired in `omega_bounds_fraction: 0.1` via `resolve_tracker_params()`, CLAUDE.md's own session-018 fix that this script had never adopted: worst omega deviation −44% → 10%, model-vs-sensor error 4.1mm antiphase → 0.231mm mean. The Q-scales-with-dt explanation for the collapse was proposed and **not confirmed** by replay, and is left open. 326 tests. |
